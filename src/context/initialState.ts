import { InitialState } from "../interface/interface";

export const initialState: InitialState = {
    currentWeather: [],
    forecast: [],
    hightlight: [],
    celcius: true,
    fahrenheit: false,
};
